apiVersion: v1
kind: Pod
metadata:
  name: react-pod
  labels:
    app: react-lbl
spec:
  containers:
    - name: begoingto-react
      image: begoingto/begoingto-doc:0.0.1
      ports:
        - containerPort: 3000